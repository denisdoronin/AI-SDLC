[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "md-formatter"
version = "0.1.0"
description = "Markdown Formatter"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"

[tool.hatch.build.targets.wheel]
packages = ["src/md_formatter"]
